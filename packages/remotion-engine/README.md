# Remotion 视频渲染

这个部分非常的单纯，就是 Remotion 的一些视频段落组件。

关于 Remotion ，请参见 [Remotion 官方文档](https://www.remotion.dev/docs)。

可以通过命令行，让 Remotion 渲染视频：

```cmd
cd packages/remotion-engine && npx remotion render ExampleComposition
```
